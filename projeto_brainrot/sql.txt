// Não sei se precisa
CREATE DATABASE projeto_brainrot;
CREATE DATABASE projeto_brainrot_teste;

CREATE TABLE conta (
	id INT PRIMARY KEY AUTO_INCREMENT,
    nome VARCHAR(50) NOT NULL
);

CREATE TABLE brainrot (
	id INT PRIMARY KEY AUTO_INCREMENT,
    id_conta INT,
    nome VARCHAR(50) NOT NULL,
    raridade VARCHAR(50) NOT NULL,
    efeito VARCHAR(200) NOT NULL,
    FOREIGN KEY (id_conta) REFERENCES conta(id)
);

SELECT id, nome FROM conta;
SELECT id, id_conta, nome, raridade, efeito FROM brainrot;

DROP TABLE brainrot, conta;